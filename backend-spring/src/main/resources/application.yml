server:
  port: 8080

spring:
  datasource:
    url: "jdbc:mysql://campuslink-db.cz84m6ekwz59.ap-southeast-2.rds.amazonaws.com:3306/campuslink?useSSL=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8"
    username: "campuslink_app"
    password: "campus1234!"
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB          # ⬅ 파일 한 개당 최대 크기
      max-request-size: 20MB       # ⬅ 요청 전체 크기 (파일 여러 개 포함)

file:
  upload-dir: uploads              # ⬅ 실제 이미지 저장 경로 (backend-spring/uploads 폴더 만들어야 함)

logging:
  level:
    org.springframework.security: INFO
    org.hibernate.SQL: DEBUG

jwt:
  secret: "REPLACE_WITH_A_32+_CHAR_RANDOM_SECRET_1234567890abcdef"
  access-exp-seconds: 3600
  issuer: "campuslink"
